source("src/paths/brunetti.R")

DPC.anagrafica_file.relative <- \(tvar, flavor) brunetti.anagrafica_file.relative("DPC", tvar, flavor)
DPC.root.relative <- \(tvar, flavor) brunetti.root.relative("DPC", tvar, flavor)
