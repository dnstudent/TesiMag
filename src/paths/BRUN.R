source("src/paths/brunetti.R")

BRUN.anagrafica_file.relative <- \(tvar, flavor) brunetti.anagrafica_file.relative("BRUN", tvar, flavor)
BRUN.root.relative <- \(tvar, flavor) brunetti.root.relative("BRUN", tvar, flavor)
